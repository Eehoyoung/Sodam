# 보안 하드닝 리포트 — 2026-06-24

> 요청: PII 암호화 저장 점검·조치 + 토큰 API 취약점 + SQLi·XSS·CSRF (BE·FE). 다중 보안감사 에이전트 + 메인 조치·라이브 검증.

## 1. PII 암호화 저장 (PIPA §29)
| 항목 | 조치 | 검증 | 커밋 |
|---|---|---|---|
| 주민번호·계좌번호 | **DB 미저장** 확인(InsuranceFiling echo만·로그 마스킹) | 코드 확인 | — |
| 🔴 암호화 키 미설정(평문 모드) | docker-compose·.env.example 키 설정/문서화 → AES-256 활성화 | `store_phone_number=enc:v1:…` 암호문 + 복호화 왕복 | cc077e2 |
| @Convert 확대 | phone+storePhoneNumber+businessLicenseNumber | — | cc077e2 |
| 운영 가드 | prod에서 키 미설정 시 부팅 거부(fail-fast) | — | cc077e2 |

## 2. 로그 PII 마스킹
| 취약 | 조치 | 검증 | 커밋 |
|---|---|---|---|
| 🔴 회원가입 비번·주민번호 평문 로깅(LogArgMasker 좌표만) | 민감필드 + 파라미터명 기반 마스킹 | 평문 비번 로그 0건(이전 2건) | cc077e2 |
| Kakao Bearer 토큰·응답본문 로깅 | 제거 | — | cc077e2 |

## 3. 토큰 API BOLA/IDOR (OWASP API #1)
클래스 `@MasterOnly`가 역할만 보고 **소유권 미검증** → storeId 변경으로 타 매장 접근. 16메서드 차단.

| 컨트롤러·메서드 | 위험 | 심각도 |
|---|---|---|
| ExportController.export* | 타 매장 출퇴근·급여 CSV 대량 덤프 | P0 |
| AttendanceCorrection.approve/reject | 타인 정정 승인=임금 조작 | P0 |
| Notification.pushToEmployee | 임의 사용자 푸시(사칭) | P0 |
| **UserController.updateEmployee** | 임의 userId 수정 + **MASTER 권한상승** | P0 |
| StoreController(직원명부·메모·할당·매장목록·운영시간) | 타 매장 데이터 | P1~P2 |
| PayrollPolicy·StorePhoto·Notification.unregisterToken | 정책 조작·사진·토큰삭제 DoS | P1~P2 |

**검증(라이브)**: 본인 매장 200 / 타 매장 403(직원·급여정책·CSV·사진·운영시간), 타인 수정 403, MASTER 승격 400. 커밋 7d036c3·3040378.

## 4. Excessive Data Exposure
- 🔴 직원명부가 raw `List<User>` 반환 → **BCrypt 비번 해시 노출**. `User.password @JsonIgnore`(광역 차단). 검증: 응답 password 키 0. (3040378)

## 5. SQLi·XSS·CSRF 결론
- **SQL 인젝션: 안전** — 전 `@Query` `:named` 파라미터 JPQL, nativeQuery 0, 사용자입력 연결 없음.
- **CSRF: 안전** — API 인증은 `Authorization: Bearer` 헤더만(쿠키 미인증). 미사용 JWT 쿠키도 Secure+SameSite=Strict 강화. (7d036c3)
- **XSS(FE WebView): 안전** — Toss customerKey=앱생성값, clientKey=config. originWhitelist는 카드사·3DS 외부 리다이렉트 필요로 '*' 유지(게이트=onShouldStartLoadWithRequest sentinel 검증).
- **FE 로깅**: api.ts 요청로그 `__DEV__` 가드(운영 비번 노출 차단), 정보화면 `Linking.openURL` http(s) 화이트리스트. (3040378)

## 6. 인간 승인/외부 잔여 (코드 무관)
- FE 토큰 AsyncStorage→EncryptedStorage/Keychain 이관(native dep)
- release APK 서명 keystore + minify/난독화(keystore=인간)
- Kakao 운영 redirect https + 운영키 주입
- 직원명부 응답 전용 DTO화(현재 password는 차단, phone/birthDate는 사장에게 노출 — product 판단)

## 7. 안전 확인(감사 본문검증)
- SSRF 0(외부호출 4곳 전부 고정호스트/config URL), Path traversal 0(ObjectStorage key=UUID)
- 노무/통계 컨트롤러 12개 가드 적용 확인, 결제 멱등·본인검증 정상, 탈퇴 PII 90일 익명화

---

## 8. 에뮬레이터 라이브 검증 (2026-06-25)

보안 수정 반영 앱을 **새로 빌드(BUILD SUCCESSFUL 2m22s)·설치** 후 emulator-5554 + Docker BE(10.0.2.2:7070)로 라이브 검증.

### 8.1 앱 라이브 구동 ✅
- ReactNativeJS 로그 정상: `Running "Sodam_Front_End"`, `API_BASE_URL: http://10.0.2.2:7070`, `[CONTEXT_READINESS] ready`, `[AuthProvider] 상태 변경 {isAuthenticated:false, loading:false}`, Firebase 로드.
- 실화면 렌더·실터치 내비게이션 작동(역할선택 "어떻게 시작할까요?" → 직원 흐름 화면). 크래시/FATAL 0.

### 8.2 FE 보안수정 = 실행 번들에 실재 ✅ (결정적 증거)
에뮬레이터가 Metro에서 로드하는 **실행 JS 번들(13.9MB)** 직접 검사:
| 수정 | 번들 검증 |
|---|---|
| 요청 body 로깅 제거(F2, 비밀번호 logcat 노출 차단) | 구패턴 `params \|\| config.data \|\| ''` = **0건**(제거 확인) |
| Linking http(s) 화이트리스트(F7) | `"안전하지 않은 링크예요"` = **3건**(3개 정보화면 적용 확인) |

> 즉, 에뮬레이터에서 실제 실행되는 코드에 비밀번호 로깅 경로가 **물리적으로 부재** → 로그인해도 logcat에 비밀번호가 찍힐 수 없음.

### 8.3 BE 보안수정 = 동일 BE 라이브 API 검증 ✅
앱이 향하는 동일 Docker BE에서: BOLA 본인200/타인403, password @JsonIgnore(응답 키 0), PII `enc:v1:` 암호문, 로그 평문비번 0건 (§3·4 참조).

### 8.4 제약(코드 무관)
- `adb shell input tap` 합성터치가 RN 신아키텍처(Fabric) 터치핸들러에 비도달(monkey 실이벤트는 도달) → UI 로그인 자동화로 logcat 한 줄 캡처는 미수행. 단 §8.2가 동일 사실을 번들 레벨에서 결정적으로 증명.
- 첫 기동 시 stale Metro(전 세션 잔존, 8090)가 부정합 번들 서빙 → fresh Metro 재기동 후 정상 로드.
