# 260816 · 포지셔닝 전환(260815) 후속 작업 계획서

> 전신 문서: `docs/260815/260815_소담_포지셔닝전환_계획서.md`(WP-1~WP-9, 전부 구현·테스트·문서 반영 완료,
> 커밋 `d97b230`~`27dbde8`). 그 작업을 마치고 나서 남은 것 — 다음 세션이 이어받을 목록이다.
>
> **분류 원칙**: 사람의 승인·전문가 회신 없이 **우리가 시간만 내면 끝나는 것**은 여기(코드 작업
> 계획서)에 WP로 남긴다. 노무사·법무 회신, 인간 승인, 출시 전 검증·산출물처럼 **외부 요인이 걸린
> 것**은 `docs/RELEASE_GATES.md`에 게이트(V-6)로 등록했다 — 이 문서와 함께 갱신한다.
> D-7(labor-health 활용 방향)은 2026-08-16 사용자 결정(안 B)으로 해소돼 WP-B로 편입됐다.

---

## 0. 왜 이 목록이 나왔는가

260815 작업은 계획서 기준 9개 WP를 전부 구현했지만, 진행 중 두 가지 종류의 갭이 새로 드러났다.

1. **백엔드는 있는데 프론트가 없다** — WP-1(상시근로자 시뮬레이터)·WP-7(노무 건강도 요약 API)
   둘 다 컨트롤러·서비스·테스트까지 완성했지만, **어느 화면도 그 엔드포인트를 호출하지 않는다**
   (`grep -rl "labor-health\|statutory-headcount" frontend/src` → 0건, 260816 재확인). 데모
   시나리오 4단계("알바 1명 추가 시 5인 경계 경고")는 사실 지금 **재현 불가능**하다 — 백엔드 API는
   있지만 그걸 부르는 UI가 없기 때문이다.
2. **의도적으로 범위를 좁힌 지점** — WP-5(개인정보 처리 차단)는 조회 전용 경고까지만 구현하고
   실제 쓰기 경로 차단은 "노무사 회신 후 판단"으로 보류했다. 이건 새로 발견한 문제가 아니라
   260815 세션에서 이미 인지하고 기록해 둔 결정인데, 실행이 안 됐으니 후속에 다시 올린다.

---

## 1. WP-A · 상시근로자 시뮬레이션 FE 배선 (P0, 데모 필수) ✅ 완료(2026-08-16)

**우선순위가 가장 높다.** 이게 안 되면 260815 작업의 핵심 데모 포인트(§7 4단계)가 안 열린다.

### 배경

- `GET /api/stores/{storeId}/labor-risk/statutory-headcount` — 현재 참고 산정 + 확대적용 로드맵.
  구현·테스트 완료(`StatutoryHeadcountServiceTest`), FE 소비처 0건
- `GET /api/stores/{storeId}/labor-risk/statutory-headcount/simulate?additionalEmployees=N` —
  직원 N명 추가 시뮬레이션(경계 초과 여부·신규 적용 조항·인건비 영향 범위). 구현·테스트 완료,
  FE 소비처 0건

### 작업

1. `frontend/src/features/risk/services/riskService.ts`에 두 엔드포인트 fetch 함수 추가
   (`fetchStatutoryHeadcount`, `simulateStatutoryHeadcount`) — 기존 `fetchLaborRisks` 패턴 그대로
2. **현재 참고 산정 + 로드맵 화면** — 신규 또는 `LaborRiskDashboardScreen` 상단에 카드로 배치.
   "이번 달 참고 산정 4.3명" + 로드맵 타임라인(`roadmap[]`, "정부가 추진 중"이라는 표현 그대로
   노출 — HC-2 위반 금지, 절대 확정 시행일처럼 보이게 재구성하지 말 것)
3. **직원 추가 등록 진입점에 시뮬레이션 훅** — 정확한 화면은 착수 시 재확인하되 유력 후보는
   `EmployeeManagementScreen.tsx`(사장의 직원 관리) 또는 `StoreDetailScreen.tsx`의 초대코드 발급
   지점. "직원을 추가하시겠어요?" 같은 확인 단계에서 `simulate?additionalEmployees=1` 결과를
   먼저 보여주고, **등록 자체는 막지 않는다**(HC-5 — 경고는 차단이 아니다. 사장이 결정)
4. 표현 규칙(HC-1·HC-2) 그대로 적용 — "5인 이상입니다" 금지, "5인 이상에 해당할 가능성이
   있습니다"만 사용. 인건비 영향은 범위(`estimatedMonthlyCostMin`~`Max`)로만 표시, 단일 금액 금지

### done_when

- [x] `riskService.ts`에 `fetchStatutoryHeadcount`·`simulateStatutoryHeadcount` 추가(jest 5건)
- [x] `LaborRiskDashboardScreen` 상단에 참고 산정 + 로드맵 카드 배치(가동일 0일이면 카드 숨김)
- [x] `EmployeeManagementScreen`에 "1명 더 채용 시 5인 경계" 경고 카드 배치 — `crossesThreshold`일
      때만 노출, 직원 초대 버튼은 항상 활성 상태 유지(HC-5 — 화면 테스트로 고정)
- [x] 표현 규칙(HC-1·HC-2) grep 0건
- [x] `npm run lint`/`npm run test:unit` green(FE 652 tests), 신규 코드에 `any` 추가 없음
- [ ] **데모 시나리오(계획서 §7) 4단계가 실제 화면 조작으로 재현됨(에뮬레이터)** — 자동테스트만
      통과, 실기 확인은 [[V-4]]·[[V-6]]에 남아 있음(에뮬레이터 필요, 이 세션에서 확인 불가)

이 WP 완료로 [[V-6]](`docs/RELEASE_GATES.md`)의 코드 조건은 충족됐다 — 남은 건 에뮬레이터 실기
확인뿐([[V-4]]·[[V-5]]와 함께 일정만 잡으면 된다).

---

## 2. WP-B · 노무 건강도(`labor-health`) 상세 분석 화면 신설 (P1) ✅ 완료(2026-08-16)

### 배경

WP-7에서 신설한 `GET /api/stores/{storeId}/labor-health`(BASIC, 건수만)·`/labor-health/detail`
(FULL, 설명까지)는 플랜 게이팅(402+errorCode) 인프라를 증명하려고 만들었다. 사장 홈 카드
(`OwnerDashboardScreen`)는 **게이팅이 없는** WP-1의 `/labor-risk`를 그대로 쓴다(데모·시드 계정
의존성을 피하려는 260815의 의도적 선택, 커밋 `d9d9c80` 메시지에 명시) — 이 배선은 그대로 둔다.

### 결정 (2026-08-16, 사용자 — [[D-7]] 해소, `docs/RELEASE_GATES.md` §5 참조)

**안 B 채택** — 홈 카드는 지금처럼 게이팅 없이 유지(항상 완전한 정보), `/labor-health`는 **별도
"노무 리스크 상세 분석" 화면**에서만 써서 실제 PRO 업셀 지점으로 삼는다.

### 작업

1. `frontend/src/features/risk/services/riskService.ts`에 `fetchLaborHealth`(BASIC)·
   `fetchLaborHealthDetail`(FULL) 추가 — 기존 `fetchLaborRisks` 패턴 그대로, 402 응답은 별도
   에러 타입으로 구분해 화면에서 페이월 분기 가능하게
2. **신규 화면 "노무 리스크 상세 분석"**(가칭, 정식 화면명은 착수 시 v3 디자인 시스템 네이밍
   확인) — `LaborRiskDashboardScreen`(기존 목록)과는 별개. 진입점은 그 목록 화면 상단 또는
   `MasterMyPageScreen` 퀵메뉴
   - BASIC(STARTER 이상): 점수(0~100, 등급처럼 크게 강조하지 말 것 — HC-1)·DANGER/WARN 건수
   - FULL(PRO 이상)만: 항목별 설명·해소 가이드
   - **FREE 또는 BASIC 계정이 FULL 필드에 접근 시** 402 → "PRO에서 자세한 설명까지 볼 수
     있어요" 류 페이월 유도 UI. 기존 `PastDueBanner`/구독 유도 컴포넌트 패턴 재사용
3. HC-1 준수 — 점수를 "안전 등급"처럼 보여주지 말 것. 화면 제목·카피는 "확인이 필요한 항목"
   중심 유지(WP-7에서 확정한 톤 그대로)

### 실제로 구현한 것

계획 당시엔 몰랐던 것 하나 — BE·FE 사이에 **전역 402 페이월 인프라가 이미 있었다**
(`common/api/client.ts`의 `setOnPlanRequired` + 앱 루트에 상시 마운트된 `PaywallHost`).
그래서 이 화면이 자체 페이월 UI를 새로 만들 필요가 없었다 — `fetchLaborHealthDetail`이 402를
던지면 axios 인터셉터가 **자동으로** 전역 페이월 시트를 띄운다. 화면은 그 요청이 실패했을 때
"화면 자체가 깨지지 않게" `fetchLaborHealth`(BASIC)로 **단계적으로 폴백**하기만 하면 됐다.

- `LaborHealthDetailScreen.tsx` 신설 — `fetchLaborHealthDetail` 시도 → 402면 `fetchLaborHealth`로
  폴백 → 그것도 402면 잠금 안내 화면(전역 페이월이 이미 뜬 상태이므로 화면 자체는 보조 안내만)
- 진입점: `LaborRiskDashboardScreen` 헤더에 "상세 분석" 액션 버튼 추가
- 네비게이션: `HomeStackParamList`에 `LaborHealthDetail: {storeId}` 등록

### done_when

- [x] FREE/STARTER 계정으로 상세 화면 진입 시 건수까지만 보이고, FULL 필드 요청 시 페이월(전역
      `PaywallHost`)로 분기되는 걸 jest로 확인(FULL/BASIC/NONE/네트워크오류 4개 시나리오)
- [x] PRO 계정에서 설명·해소 가이드까지 정상 노출
- [x] `npm run lint`/`npm run test:unit` green
- [ ] 실제 PRO/STARTER/FREE 시드 계정으로 에뮬레이터 실기 확인 — 미착수(다음 세션)

---

## 3. WP-C · minorguard 개인정보 처리 실제 차단 (P1, G-17 회신 후 착수)

### 배경

260815 WP-5에서 `MinorLaborGuardService.evaluate()`가 만 14세 미만 + 친권자 동의서 미확인 시
`personalDataProcessingBlocked=true` 경고 플래그를 반환하도록 구현했지만, 이 서비스는 **조회
전용**이라 실제 등록·근무기록 등 **쓰기 경로를 코드로 차단하지 않는다**(코드 주석에 명시,
`MinorLaborGuardService.java` 클래스 javadoc 참조).

### 왜 지금 안 하는가

어느 쓰기 경로까지 하드 차단을 걸지가 그 자체로 노무사·법무 판단 영역이다(G-17). 잘못 넓게
막으면 정상적인 미성년 직원 등록 자체가 막히고, 잘못 좁게 막으면 §22의2 요건을 충족 못 한다.

### G-17 회신 후 작업

1. 회신에서 확정된 범위에 맞춰 차단 지점 특정(후보: 직원 초대코드 승인 시점, 출퇴근 최초 기록
   시점, 근로계약서 생성 시점 — 회신에 따라 달라짐)
2. 차단 시 사장에게 노출할 안내 문구(HC-1 톤 유지, "차단됐습니다"가 아니라 "먼저 동의서를
   등록해 주세요" 류)
3. 이미 등록된 만 14세 미만 직원(동의서 미확인) 소급 처리 정책 필요 — 기존 데이터 마이그레이션이
   아니라 **운영 판단**(강제 비활성화 vs 경고만 유지) 필요, 이것도 G-17 범위에 포함해 함께 질의

---

## 4. WP-D · 가격 A/B 실험 그룹 배정 로직 설계 (P2) ✅ 배정 로직 완료(2026-08-16), 집계 쿼리는 미착수

### 배경

WP-8에서 `Subscription.priceVariant` 컬럼(A/B 가격 실험 그룹)을 마련했지만 **어디서도 실제
배정하지 않는다**(`grep -rn "assignPriceVariant" backend/src/main` → 0건, 260816 확인). 값
자체(H-7)와 무관하게 **배정 메커니즘**은 지금 설계해도 된다 — 결정되지 않은 건 "얼마로 보여줄까"
지 "누구를 A/B로 나눌까"가 아니다.

### 작업 (가격 숫자 변경 없이 진행 가능)

1. [x] 배정 기준 설계 — `PlanPricingService.assignVariant(userId)`, Knuth 곱셈 해시로 userId를
   섞어 결정적 50:50 "A"/"B" 배정(같은 userId는 항상 같은 그룹)
2. [x] `SubscriptionService.subscribe()`·`subscribeFree()` 양쪽에서 `assignPriceVariant(...)`
   호출 지점 추가 — 유료·무료 가입 전부 그룹이 배정됨
3. **주의(계속 유효)**: 그룹을 나누는 것과 그룹별로 다른 가격을 청구하는 것은 다른 일이다. 지금
   `PlanPricingService.currentCatalogPriceKrw`/`effectivePriceKrw`는 이 배정과 완전히 무관하게
   항상 같은 카탈로그 가격만 반환한다 — 그룹별 차등 청구는 아직 어디에도 없다(H-7 승인 이후)
4. [ ] **미착수** — 실험 결과 집계 쿼리(어느 그룹의 전환율이 높은지). `DomainEventService` 기존
   인프라 재사용 검토, 신규 집계 테이블 만들기 전에 먼저 확인. 실제 A/B 테스트를 돌리기 전까지는
   급하지 않음(배정 라벨만 쌓여 있어도 나중에 집계 가능)

### 검증

- [x] `PlanPricingServiceTest`에 4건 추가 — 결정적 배정(같은 userId→같은 그룹), null-safe,
      50개 userId 배정 시 A/B 양쪽 다 나옴(한쪽 쏠림 없음)
- [x] `SubscriptionBillingIntegrationTest`에 2건 추가 — 유료·무료 가입 둘 다 priceVariant 배정 확인
- [x] `.\gradlew test` green(BE 전량)

---

## 5. WP-E · 신규 화면 테스트 커버리지 보강 (P2, 낮은 위험도) 🔶 부분 완료(2026-08-16)

260815에서 서비스·컨트롤러 레벨 테스트는 촘촘히 붙였지만(BE 신규 테스트 다수, FE는 서비스
레벨만) 화면 렌더 레벨 jest 테스트가 빠진 곳이 있었다. WP-A·WP-B 작업 중 **새로 만들거나 손댄
화면**(회귀 위험이 가장 큰 곳)은 그 자리에서 함께 테스트를 붙였다 — 아래는 갱신된 현황.

| 화면 | 현재 상태 |
|---|---|
| `LaborRiskDashboardScreen.tsx` | ✅ `riskService.test.ts`(5건) + `LaborRiskDashboardScreen.test.tsx`(3건, 상시근로자 카드 노출/숨김/best-effort) |
| `LaborHealthDetailScreen.tsx`(신규) | ✅ `LaborHealthDetailScreen.test.tsx`(4건 — FULL/BASIC/NONE/네트워크오류 4경로 전부) |
| `EmployeeManagementScreen.tsx` | ✅ `EmployeeManagementScreen.test.tsx`(4건 — 5인 경계 경고 노출/숨김/best-effort/managerMode 미조회) |
| `MyCertificateScreen.tsx` | 🔶 **미착수** — 화면 렌더 테스트 없음(백엔드 `CertificateServiceTest`는 있음) |
| `MinorGuardScreen.tsx` | 🔶 **미착수** — 화면 렌더 테스트 없음(서비스 테스트는 WP-5에서 추가함) |

남은 둘(`MyCertificateScreen`·`MinorGuardScreen`)은 이번 세션에서 코드를 건드리지 않은 화면이라
급하지 않다 — 수동 검증(lint+tsc+기존 통합 테스트)은 이미 통과했다. 회귀 방지 차원의 보강 작업으로
남겨둔다.

---

## 6. 우선순위 요약 (2026-08-16 갱신)

| WP | 우선순위 | 상태 | 남은 것 |
|---|---|---|---|
| A. 상시근로자 시뮬레이션 FE 배선 | **P0** | ✅ 코드 완료 | 에뮬레이터 실기 확인([[V-4]]·[[V-6]]) |
| B. labor-health 상세 분석 화면 신설 | P1 | ✅ 코드 완료 | 에뮬레이터 실기 확인(PRO/STARTER/FREE 계정) |
| C. minorguard 쓰기경로 차단 | P1 | 🔶 보류 | **G-17 회신 대기** |
| D. 가격 A/B 배정 로직 | P2 | ✅ 배정 로직 완료 | 실험 결과 집계 쿼리(급하지 않음) |
| E. 화면 테스트 보강 | P2 | 🔶 부분 완료 | `MyCertificateScreen`·`MinorGuardScreen` 렌더 테스트 |

### 부수 발견 — RetentionNoticeServiceTest 잔여 플레이키니스

260815 세션에서 `@DirtiesContext(BEFORE_CLASS)`로 1차 조치했던 `RetentionNoticeServiceTest`가
260816 전체 스위트 재실행에서 **다시 간헐적으로 재현**됐다(여전히 단독/패키지 실행은 100% 통과).
클래스 단위 격리로는 못 막는 오염 경로가 남아있다는 뜻이라, 이번엔 `BEFORE_EACH_TEST_METHOD`로
격리 강도를 최대로 올렸다 — 근본 원인(어느 클래스가, 어떤 경로로 오염시키는지)은 여전히
**미확정**이다. 이 파일이 계속 간헐적으로 빨개지면 다음 세션은 원인부터 다시 본다(가드를
느슨하게 바꾸는 임시방편 금지 — `.claude/rules/testing.md`).
