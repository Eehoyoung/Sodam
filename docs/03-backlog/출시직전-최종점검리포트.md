# SODAM 출시 직전화 — 최종 점검 리포트

**일자**: 2026-06-05
**범위**: FE·BE 통합 점검 및 종합 하드닝 (29 컨트롤러 / ~176 엔드포인트 / FE 전 영역)

---

## 1. 빌드·테스트·타입체크 (DoD1)

| 항목 | 결과 |
|---|---|
| BE `gradlew build` (컴파일) | ✅ 통과 |
| BE `gradlew test` | ✅ **209 통과 / 0 실패** (플래키 1건 결정론화) |
| FE `tsc --noEmit` (타입체크) | ✅ **0 error** |
| FE `jest` | ✅ **200 통과 / 0 실패** (14 skip) |
| FE `eslint` | ⚠️ 487 error — 대부분 design-system-v2 WIP. tsc·테스트 그린이라 출시 차단 아님. 조치법 인계서 B-1 |

## 2. 엔드포인트 점검 매트릭스 (DoD2)
- 전수 기록: [엔드포인트 점검 매트릭스](./엔드포인트-점검-매트릭스.md) — 29 컨트롤러를 [동작확인/수리됨/해당없음]으로 분류.
- 권한(@MasterOnly/@EmployeeOrMaster/@AnyAuthenticated) + 도메인 가드(StoreAccessGuard) 일관성 확인.

## 3. 코드 수리 결함 (DoD3)

### 본 세션 수리·테스트 완료
| 우선순위 | 결함 | 수리 |
|---|---|---|
| 🔴 보안(IDOR) | StoreQueryController 전테넌트 매장·통계 무제한 노출(@EmployeeOrMaster) | @MasterOnly + 소유권/본인 가드, 전테넌트 목록·검색 엔드포인트 제거 |
| 🔴 보안(IDOR) | StoreStatsController master 간 타 매장 통계 조회 | 소유권 가드 추가 |
| 🟠 성능 | StoreStats monthToDate `payrollRepository.findAll()` 전테넌트 로딩 | 매장 스코프 쿼리로 교체 |
| 🟠 크래시 | useDimensions.ts hook을 try/catch로 감싸 rules-of-hooks 위반 | 외부 try 제거(hook 최상위화) |
| 🟡 품질 | 플래키 테스트(자정 롤오버) | 결정론화 |

### 보류(인계서 B) — 코드 가능하나 충돌/검증 리스크로 정확히 문서화
- FE 린트 487건(design WIP, `--fix` 130 자동가능), react-hooks 6파일(RN 런타임 검증 필요),
  PersonalUserController self-검증 강화, 원시 엔티티→DTO 전환.

## 4. 잔여 인계 (DoD4)
- [출시 직전 잔여 인계서](./출시직전-잔여인계서.md) — 코드 불가(약관문구·행정신고·결제고지·마이그레이션 실행·운영배포)
  + 코드 가능 보류분을 ①항목②사유③코드연동④사람할일로 전수 기록.

## 5. 종합 판정

**"출시 직전 상태" 도달.**
- 코드로 닫을 수 있는 **보안(IDOR)·데이터 격리·크래시·성능·정합성** 결함은 수리·테스트 완료.
- 핵심 도메인(급여/출퇴근/시급/Auth) 동작·권한·테스트 그린.
- 잔여는 (a) 변호사·행정·결제·배포 등 **인간/외부 절차** 또는 (b) WIP 충돌·RN 런타임 검증이 필요한
  **품질 정리** — 모두 인계서에 조치법까지 문서화.

출시 전 인간 액션 핵심 3종: **약관 문구 확정(변호사) · 위치기반서비스 신고(행정) · 운영 배포/마이그레이션 실행 승인.**

> 코드 변경은 로컬 커밋만 수행(push·배포는 인간 승인). 본 goal 산출물 4종: 매트릭스 / 잔여 인계서 / 최종 리포트 / 보안·크래시 수리 커밋.
