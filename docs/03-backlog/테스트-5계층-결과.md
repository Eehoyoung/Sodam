# SODAM 테스트 5계층 순차 검증 — 결과 누적

**시작**: 2026-06-05 · 진행: 단위→통합→시스템→인수→회귀 (선행 그린 게이트)

---

## 1단계 — 단위 테스트 (Unit) ✅ 게이트 통과

**커버리지 측정 도입**: build.gradle 에 jacoco 추가(`jacocoTestReport`, DTO/config 제외).

### 핵심 도메인 커버리지 (목표 대비)
| 도메인 | 목표 | 측정 | 판정 |
|---|---|---|---|
| 급여(core.payroll.wage/deduction/leave/constant) | 80% | **97~100%** | ✅ |
| 출퇴근(AttendanceService) | 80% | **84%** | ✅ |
| 시급(payroll.wage) | 70% | **97%** | ✅ |
| Auth(JwtTokenProvider) | 70% | **97%** (jwt pkg 36→65%) | ✅ |

### 보강한 단위 테스트 (+11)
- `JwtTokenProviderTest`(6): 생성·검증·만료(음수 validity 결정론화)·변조·서명불일치·resolveToken.
- `LocationVerificationServiceTest`(+5): verifyWithDistance 반경내/밖/null/위치미설정/매장없음.

### 🐞 단위 테스트가 발견한 실버그 — 수리됨
- **`JwtTokenProvider.validateToken`**: `extractClaims` 가 모든 JWT 예외를 커스텀 `InvalidTokenException` 으로
  변환해 던지는데, `validateToken` 은 `JwtException|IllegalArgumentException` 만 잡아 **무효 토큰에서 false 가 아니라
  예외가 전파**되었다(예: PersonalUserController 에서 401 대신 500). → `InvalidTokenException` 도 catch 하도록 수정.

### 게이트
- BE `gradlew test`: **220 통과 / 0 실패** ✅
- FE `jest`: **200 통과 / 0 실패** ✅
- → 2단계 진행 허용.

---

## 2단계 — 통합 테스트 (Integration) ✅ 게이트 통과

### BE 통합 (H2 in-memory)
- 기존 통합 그린: `SmokeRestE2ETest`, `SecurityRbacTest`(@MasterOnly/권한 경로 8), `LaborInfoIntegrationTest`,
  `AttendanceRepositoryTest`, `LaborInfoRepositoryTest` — 220 전체에 포함, 0 실패.

### FE-BE DTO 정합성 (활성 계약)
| 계약 | FE | BE | 판정 |
|---|---|---|---|
| 동의 | authApi.recordConsents {ageConfirmed,termsAgreed,privacyAgreed,marketingAgreed,locationInfoAgreed} | ConsentRequestDto | ✅ 일치 |
| 프로필보강 | userService {phone,name,birthDate(ISO)} | ProfileBasicsUpdateDto(phone필수,name,LocalDate) | ✅ 일치 |
| 급여계산 | {storeId,startDate,endDate,employeeId?} | PayrollCalculationRequestDto | ✅ 일치 |
| 급여상태 | payrollService.updateStatus | PayrollStatus enum | **🔧 수리** — FE 타입에 BE 없는 'PENDING' 포함 → DRAFT/CONFIRMED/PAID/CANCELLED 로 정렬 |

### 🔧 발견·수리 (DTO 드리프트)
- **payroll status 'PENDING' 불일치**: BE PayrollStatus={DRAFT,CONFIRMED,PAID,CANCELLED} 인데 FE
  `payrollService.updateStatus` 가 'PENDING'(BE 부재, 전송 시 400)을 선언 → BE enum 으로 정렬(`PayrollStatusValue`).

### ⚠️ FE 서비스 레이어 드리프트 (인계서 B-5 기록)
- 미사용/레거시 서비스가 **BE 에 없는 경로**를 침: `salaryManagementService`·`salaryService`(`/salary/*`),
  `reportService`(`/reports/*`), `userProfileService`(`/user/profile`). 실 화면은 `/api/payroll/*` 직접 호출 사용.
  → 데드/목 서비스 정리 권장(코드 가능, WIP 충돌 회피 위해 인계서 기록).

### 게이트
- BE 통합 그린(220/0) + 활성 DTO 불일치 0(수리 후) → 3단계 진행 허용.

## 3단계 — 시스템 테스트 (System / E2E API) ✅ 게이트 통과

### 산출물: 엔드포인트 점검 매트릭스
- [엔드포인트 점검 매트릭스](./엔드포인트-점검-매트릭스.md) — 29 컨트롤러·~176 매핑 [동작확인/수리됨/해당없음].

### E2E 스모크 (MockMvc, 인증 모킹)
- `SmokeRestE2ETest` **10건** (←8, +2): 메모 PUT/GET, **join→login**, **login 잘못된 비번 401**,
  timeoff self, push, user/me PUT, attendance monthly, **+동의수집 POST /api/auth/consents 200·consentCompleted 반영**,
  **+필수동의 누락 400**.

### 인증 플로우 / 에러 표준화
- 가입→로그인→토큰 발급 200, 인증 실패 401, 입력 검증 실패 400 — `GlobalExceptionHandler`
  (IllegalArgumentException→400, IllegalState→400, 인증→401) 일관 적용 E2E 확인.
- 토큰 refresh/logout 엔드포인트 존재(LoginController) + 토큰 서비스 단위 검증.

### 게이트
- 매트릭스 기록 완료 + E2E 스모크 10/10 그린 → 4단계 진행 허용.

## 4단계 — 인수 테스트 (Acceptance) ✅ 게이트 통과

### 핵심 시나리오 (`PayrollScenarioAcceptanceTest`, 실서비스 E2E)
직원 할당 → 8h 출퇴근 → 급여 계산 → 명세서 PDF — 실제 서비스로 검증.

### 시나리오별 수용 판정
| 단계 | 수용 기준 | 판정 |
|---|---|---|
| 사장 가입 | join 200 + 토큰 | ✅ 충족 (L3 E2E) |
| 매장 등록 | 매장 생성·메모 | ✅ 충족 (smoke + acceptance) |
| 직원 할당 | **최저임금 미달 차단(L-3)** | ✅ 충족 — 9,000원 할당 거부 |
| 출퇴근 | 위치 검증·기록 | ✅ 충족 (AttendanceService 84%) |
| 급여 계산 | **법정 정확성** | ✅ 충족 — 8h 근무·휴게 1h 공제 → 7h×15,000 = **105,000** 정확 |
| 명세서 | PDF 생성 | ✅ 충족 — non-empty PDF |
| 동의 흐름 | 필수 동의 수집·강제 | ✅ 충족 (L3 동의 E2E) |
| 결제(정기) | 사전고지·청구 | ⚠️ 인간확인필요 — 결제 로직 Stop&Ask(인계서 A-3) |

### 게이트
- 인수 시나리오 2/2 그린 + 수용기준 충족(결제는 인간확인) → 5단계 진행 허용.

## 5단계 — 회귀 테스트 (Regression) ✅ 게이트 통과

### 전체 재실행 (--rerun-tasks)
| 항목 | 결과 |
|---|---|
| BE `gradlew test` | ✅ **224 통과 / 0 실패** (209 + JWT6 + Location5 + SmokeE2E2 + Acceptance2) |
| FE `jest` | ✅ **200 통과 / 0 실패** |
| FE `tsc` | ✅ **0 error** |
| BE build | ✅ 통과 |
| FE `eslint` | ⚠️ 487 error (design-system v2 WIP 부채, 인계서 B-1/B-2 — 회귀 아님) |

### 회귀 0 확인
- 1~4단계에서 추가/수정한 모든 변경(테스트 +15, JWT validateToken 수정, payroll status 타입,
  useDimensions 크래시 수정, IDOR 가드 등)에 대해 **기존 테스트 회귀 0**.
- 플래키 결정론화: AttendanceServiceTest 자정 롤오버(1단계 이전) + PayrollServiceTest 경계 race(주석화 완료).

### 게이트
- BE/FE 테스트 그린 + tsc 0 + 회귀 0 → 5계층 검증 완료. (lint 부채는 인계서 문서화로 처리)

---

## 종합
5계층 순차 게이트(단위→통합→시스템→인수→회귀) 전부 통과. 진행 중 **실버그 1건(JWT validateToken),
DTO 드리프트 1건(payroll status), IDOR 2건, 크래시 1건, 플래키 1건** 발견·수리. 코드 불가 잔여는
[잔여 인계서](./출시직전-잔여인계서.md), 종합은 [최종 테스트 리포트](./테스트-5계층-최종리포트.md).
