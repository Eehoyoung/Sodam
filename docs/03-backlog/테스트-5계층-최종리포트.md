# SODAM 테스트 5계층 — 최종 종합 리포트

**일자**: 2026-06-05
**대상**: 단위 → 통합 → 시스템 → 인수 → 회귀 (순차 게이트, 선행 그린 시에만 다음 진행)
**판정**: **5계층 전부 게이트 통과. "테스트로 검증된 출시 직전 상태" 도달.**

---

## 1. 계층별 결과 요약

| 계층 | 핵심 활동 | 결과 | 게이트 |
|---|---|---|---|
| **1 단위** | jacoco 도입, 핵심 도메인 커버리지 측정·보강 | 급여 97~100% · 출퇴근 84% · 시급 97% · **Auth 36→97%** | ✅ BE 220·FE 200 그린 |
| **2 통합** | 컨트롤러↔서비스↔리포(H2) + FE-BE DTO 정합성 | 활성 DTO 정합, 드리프트 1건 수리 | ✅ 통합 그린 + 불일치 0 |
| **3 시스템** | 엔드포인트 E2E + 인증 플로우 + 에러 표준화 | 매트릭스(29컨트롤러) + E2E 스모크 10/10 | ✅ 스모크 그린 |
| **4 인수** | 핵심 시나리오(가입→매장→직원→출퇴근→급여→명세서) | 급여 정확성 105,000 + 최저임금 차단 검증 | ✅ 인수 2/2 그린 |
| **5 회귀** | 전체 BE+FE 재실행, 빌드·tsc·린트 | BE 224·FE 200 그린, tsc 0, 회귀 0 | ✅ 회귀 0 |

---

## 2. 최종 테스트 현황

| 항목 | 수치 |
|---|---|
| BE 테스트 | **224 통과 / 0 실패** (5계층 진행 중 +15) |
| FE jest | **200 통과 / 0 실패** |
| FE 타입체크(tsc) | **0 error** |
| BE 빌드 / 컴파일 | 통과 |
| FE 린트(eslint) | 487 error — design-system v2 WIP 부채(인계서 B-1/B-2, 회귀 아님·차단 아님) |

---

## 3. 5계층 진행 중 발견·수리한 결함 (우선순위순)

| 우선 | 결함 | 계층 | 수리 |
|---|---|---|---|
| 🔴 보안(Auth) | **JwtTokenProvider.validateToken**: extractClaims 가 던진 InvalidTokenException 미포착 → 무효 토큰에서 false 대신 예외 전파(401→500) | 1 단위 | catch 추가 |
| 🟠 데이터 정합성 | **payroll status 'PENDING'**: BE PayrollStatus 부재값을 FE 가 전송 가능(400) | 2 통합 | BE enum 정렬 |
| 🔴 보안(IDOR) | StoreQueryController 전테넌트 노출 / StoreStatsController master 간 조회 | (직전 goal) | 소유권·self 가드 |
| 🟠 크래시 | useDimensions try-wrapped hooks(rules-of-hooks) | (직전 goal) | 외부 try 제거 |
| 🟡 품질 | 플래키(자정 롤오버) | 1 단위 | 결정론화 |

> ※ 단위 테스트(L1)가 실제 인증 버그(validateToken)를, 통합(L2)이 DTO 드리프트를 잡아냄 — 계층별 검증의 실효성 입증.

---

## 4. 핵심 도메인 커버리지 (jacoco, INSTRUCTION 기준)

| 도메인 | 목표 | 측정 | 판정 |
|---|---|---|---|
| 급여(core.payroll.*) | 80% | 97~100% | ✅ |
| 출퇴근(AttendanceService) | 80% | 84% | ✅ |
| 시급(payroll.wage) | 70% | 97% | ✅ |
| Auth(JwtTokenProvider) | 70% | 97% | ✅ |

---

## 5. 잔여 (코드 불가 / 보류 — 인계서에 조치법 문서화)

- **코드 불가(인간/외부)**: 약관 법률문구(변호사)·위치기반서비스 신고(행정)·정기결제 사전고지(결제 승인)·
  마이그레이션 실행·운영 배포 — [잔여 인계서 A](./출시직전-잔여인계서.md).
- **코드 가능·보류(사유·조치법 명시)**: FE 린트 487(WIP 충돌 회피, `--fix` 130 자동가능),
  react-hooks 6파일(RN 런타임 검증 필요), FE 레거시 서비스 드리프트(미사용 경로) — 인계서 B-1·B-2·B-5.

---

## 6. 종합 판정

**5계층 순차 검증 완료 — 코드로 닫을 수 있는 결함은 수리·테스트, 코드 불가/보류는 전수 문서화.**
- 핵심 가치사슬(가입·매장·직원·출퇴근·급여·명세서·동의)이 단위→인수까지 일관 그린.
- 법정 급여 계산 정확성(휴게 공제·최저임금 차단) 인수 검증.
- 출시 전 인간 액션 3종은 변동 없음: 약관 문구(변호사)·위치기반 신고(행정)·운영 배포/마이그레이션 승인.

**산출물**: 5계층 결과(`테스트-5계층-결과.md`) · 엔드포인트 매트릭스 · 잔여 인계서 · 본 최종 리포트.
**커밋(로컬)**: L1 `…`, L2 `4504492`, L3 `8640f9f`, L4 `…` 등 단계별 분리(push 보류).
