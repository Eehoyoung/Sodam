# 출시 직전 잔여 인계서 (출시 직전화 goal — DoD4)

**작성일**: 2026-06-05
**판정**: 코드로 닫을 수 있는 결함은 수리·테스트 완료. 아래는 ① 코드로 닫을 수 없는 항목(인간/전문가/외부)과
② 코드 수리 가능하나 본 세션에서 의도적으로 보류한 항목(사유·정확한 조치법 명시)이다.

> 형식: **① 항목 ② 사유 ③ 코드 연동 지점 ④ 사람이 할 일**

---

## A. 코드로 닫을 수 없음 — 인간/전문가/외부 절차

### A-1. 약관·면책 법률 문구 (변호사)
- **사유**: CLAUDE.md 하드룰 — 약관/개인정보/면책 "법률 문구" 자율 작성 금지(법적 리스크).
- **코드 연동**: `ConsentBlock.legalTexts`(FE) / `docs/legal/*.md` 초안 / `TermsVersions.java` 버전 식별자.
- **사람이 할 일**: 이용약관·개인정보·위치기반·마케팅 4종 + 면책조항 확정 → `legalTexts` 주입. → 상세: [G1-G2 인수인계](./G1-G2-출시차단-인수인계.md)

### A-2. 위치기반서비스사업·통신판매업 행정신고 (행정사/대표자)
- **사유**: 정부 행정 절차(코드 아님). 위치정보법 §15·§19.
- **코드 연동**: GPS 출퇴근(`AttendanceService`) — 동의 강제 가드는 구현됨. 신고는 별개.
- **사람이 할 일**: 방통위/KISA 위치기반서비스사업 신고 + 통신판매업 신고 접수.

### A-3. 정기결제 사전고지 + 결제/과금 로직
- **사유**: CLAUDE.md — 결제/과금 로직 변경 = 인간 승인(Stop&Ask).
- **코드 연동**: `SubscriptionController`(/api/billing), `TossWebhookController`(HMAC 검증됨).
- **사람이 할 일**: 정기결제 사전고지 정책·문구 확정 후 결제 플로우 반영 승인.

### A-4. DB 마이그레이션 "실행" (운영 DBA/대표자)
- **사유**: CLAUDE.md — 마이그레이션 작성은 OK, 실행은 인간 승인. 운영 데이터 영향.
- **코드 연동**: `backend/src/main/resources/db/migration/V3~V9__*.sql` (작성 완료).
- **사람이 할 일**: 스테이징 검증 후 운영 Flyway 마이그레이션 실행 승인.

### A-5. 운영 배포 / 시크릿 주입 (운영)
- **사유**: CLAUDE.md — 운영 배포·외부 키 = 인간 승인.
- **코드 연동**: `application-prod.yml.example`(PII 암호화 키 `sodam.security.pii.encryption-key` 등 자리만).
- **사람이 할 일**: AWS Secrets Manager 키 주입 + 배포.

---

## B. 코드 수리 가능하나 본 세션 보류 — 사유·조치법 명시

### B-1. FE ESLint 부채 487 error (design-system-v2 WIP)
- **사유**: 대부분 진행 중(미커밋) 디자인시스템 v2 파일에 분포. 사용자가 활성 편집 중인 WIP를
  bulk 수정하면 작업 충돌 위험. 타입체크(tsc)는 0 error, 테스트는 그린이라 **출시 차단은 아님**.
- **분포(룰별)**: no-unused-styles 173, curly 103, no-unused-vars 54, prefer-nullish-coalescing 51,
  no-duplicate-imports 28, no-unnecessary-type-assertion 20, eqeqeq 17, no-var-requires 9, 기타.
  이 중 **130건은 `eslint --fix` 자동수정 가능**.
- **코드 연동**: `frontend/.eslintrc.js`(룰 강화로 표면화), `frontend/src/**`.
- **사람이 할 일**: 디자인시스템 v2 작업 일단락 후 `cd frontend && npx eslint "src/**/*.{ts,tsx}" --fix`
  실행(130건 자동해소) → 잔여 수동수정. WIP 충돌 방지를 위해 작업자 본인이 타이밍 선택 권장.

### B-2. react-hooks/rules-of-hooks 크래시 위험 6파일 17건
- **사유**: hook(useMemo 등)을 try/catch·조건부로 호출 → 중간 throw 시 hook 수 불일치로
  "Rendered fewer hooks" 런타임 크래시 가능. **수정 패턴은 명확**(외부 try 제거, 가드는 내부로)하나,
  애니메이션/JSI worklet·레이아웃 코어라 **실제 RN 앱 실행 검증 없이 6파일 일괄 수정은 위험**.
- **참조 수정(완료)**: `src/common/hooks/useDimensions.ts` — 외부 try/catch 제거로 4건 해소(본 세션, 테스트 통과). 동일 패턴 적용.
- **대상 파일**:
  - `src/hooks/useJSISafeDimensions.ts` (5) — useDimensions.ts와 동일 패턴(거의 중복)
  - `src/common/components/layout/MainLayout.tsx` (3)
  - `src/utils/animations/animationHooks.ts` (3)
  - `src/common/components/feedback/Modal.tsx` (2)
  - `src/common/components/feedback/Toast.tsx` (2)
  - `src/components/animations/JSISafeAnimatedComponents.tsx` (2)
- **사람이 할 일**: 각 파일에서 hook을 try/catch 밖 최상위로 이동(useDimensions.ts 참고) → `npm run android`로
  화면·애니메이션 런타임 검증 → 회귀 확인.

### B-3. PersonalUserController 경로 userId 본인 검증 — ✅ 점검 결과 이미 적용(정상)
- **점검 결과**: 11개 전 엔드포인트가 `verifyAccess(userId, request)` 호출 → 토큰 userId == 경로 userId(본인)
  또는 ROLE_MASTER/MANAGER 만 허용. IDOR 가드 **이미 구현됨**. 별도 조치 불필요.

### B-5. FE 서비스 레이어 드리프트 — BE 부재 경로 침 (미사용/레거시 정리)
- **사유**: 일부 FE 서비스가 BE 에 존재하지 않는 엔드포인트를 호출(런타임 404). 대부분 화면에 미연결(데드/목).
  실 화면은 `/api/payroll/*` 직접 호출로 동작 → 출시 차단은 아니나 혼란·미연결 위험.
- **대상**: `myPage/services/salaryManagementService.ts`·`salary/services/salaryService.ts`(`/salary/*`),
  `myPage/services/reportService.ts`(`/reports/*`), `myPage/services/userProfileService.ts`(`/user/profile`),
  `attendanceService.ts` 의 `/api/attendance/batch-status`(BE 부재).
- **사람이 할 일**: 미사용 서비스 제거 또는 BE 엔드포인트 신설 결정 후 정리(디자인시스템 작업과 함께).
- **본 세션 수리**: `salary/services/payrollService.ts` updateStatus 타입 'PENDING' 제거(BE enum 정렬).

### B-4. 원시 엔티티(Store/Attendance/Payroll) 직접 반환 → DTO 전환 (점진)
- **사유**: 일부 조회가 JPA 엔티티를 직렬화(지연로딩·과노출 위험). 광범위 리팩토링이라 점진 권장.
- **코드 연동**: StoreQueryController(Store), 일부 통계/조회 컨트롤러.
- **사람이 할 일**: 응답 DTO 도입 로드맵 수립 후 도메인별 전환.

---

## 본 세션 수리 완료 (참고)
- 보안 IDOR 2건: StoreQueryController(@EmployeeOrMaster→@MasterOnly+소유권/본인 가드, 전테넌트 목록·검색 제거),
  StoreStatsController(소유권 가드 + findAll()→매장 스코프 쿼리).
- 크래시 1건: useDimensions.ts try-wrapped-hooks 제거.
- 플래키 테스트 1건: AttendanceServiceTest 중복검출 자정 롤오버 → 결정론화.
- (이전 커밋 `09c8823`) 노무 L-1~L-4 + 법무 G-1~G-4 코드분 + 명세서·동의·PII.
