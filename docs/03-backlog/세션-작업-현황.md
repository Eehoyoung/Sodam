# 🔴 세션 작업 현황 (재시작 시 여기부터) — LIVE

> **목적**: 토큰 소진으로 세션이 끊겨 재시작해도 **이 문서만 보면 즉시 이어서 작업** 가능.
> **갱신 규칙**: 매 마일스톤(에이전트 완료·트랙 완료·검증 통과)마다 메인이 이 문서를 갱신한다.
> **최종 갱신**: 2026-06-12

---

## ★ 진행 중 프로그램: 전면 재디자인(토스식) — DoD 충족까지 LOOP
**완료 조건(전부 충족 시에만 종료, 미충족이면 다음 웨이브 반복):**
1. DS v3 파운데이션 컴포넌트 업그레이드(AppButton/Card/Text/ScreenContainer + AmountText/HeroNumber/StepScaffold) — green.
2. **전 화면(59개) v3 재디자인**(한화면1가지·히어로숫자·여백·하단CTA1·스텝·이모지→아이콘·토큰만·다크모드).
3. 매 단계 게이트: BE gradlew test 268 · FE tsc 0 · jest 0fail · eslint 0error.
4. 스펙(`docs/05-design/DESIGN_SYSTEM_V3_TOSS.md`)·본문서 동기화 + 커밋.
**루프 메커니즘**: 백그라운드 에이전트 완료가 메인 재호출 → DoD 검사 → 미충족이면 다음 클러스터 에이전트 가동. W1(파운데이션)→W2(클러스터 C1~C5 병렬).
**현재 단계**: ✅ **DoD 충족 — 전면 재디자인 완료.** W1(DS v3 파운데이션)+W2 전 클러스터(C1 홈·출퇴근 / C2 급여·구독 / C3 매장·설정·운영 / C4a 온보딩·로그인 / C4b 마이페이지[PersonalUser·MasterMyPage 대형 마이그레이션] / C4c 정보·시스템) = **약 59화면 토스식 v3**. 구독카드 이모지→Ionicons 마감. 통합검증 **tsc 0·jest 221·eslint 0**, BE 268(미변경). 남은 건 커밋 + 외부/인간 게이트뿐.

---

## 0. 지금 당장 할 일 (RESUME POINT)
> 대부분 완료. **BE 268 그린·FE tsc0/jest221/eslint0**. 사장 플로우 전면 배선 완료(§8). 매장설정·운영시간·시급이력 완비.
1. **디자인 픽셀수정 에이전트(a95c68d2) 완료 대기** → 완료 시 FE 재검증(tsc/jest/eslint) + §8 P2 갱신.
2. 검증 게이트: `backend: ./gradlew test`(268) · `frontend: npx tsc --noEmit && npx jest && npx eslint . --ext .ts,.tsx` → 회귀 0.
3. 문서 동기화(§5): 엔드포인트 매트릭스에 신규(운영시간·시급이력·직원활성·세무주문·4대보험·집계·구독 일시정지) 추가, 출시-준비도-종합 GO 갱신.
4. (요청 시) 트랙별 `[MASTER]/[BLOCKER]` 로컬 커밋.
5. **잔여(코드불가/대형)**: 외부키·계약·행정·심사(§6) + P2 디자인 대형 2건(PersonalUserScreen·MasterMyPage 팔레트).

---

## 1. 현재 목표 (활성 /goal)
**소담을 "출시 직전(RC)"로 완성** — 모든 버튼이 엔드포인트/스크린에 연결, 인수 E2E 그린, 코드로 닫을 항목 0(외부·인간 게이트만 잔여). 실키 연동(토스/국세청/공단 EDI/FCM/카카오)은 **mock+key-ready로 충족 간주**(실호출 면제).
**추가 지시**: 모든 버튼 연결 / PM·FE 협력 디자인·픽셀 검증 / PO·소상공인 에이전트로 필요기능 자율 추가 / 매장설정 페이지(시급·주소).
**근거 문서**: 마스터 > 수익화확정안 v3.1 > PRD(+역할별) > IDENTITY(Non-Goal) > FEATURES. 충돌 시 이 순서.

## 2. 검증 게이트 현재 수치 (마지막 측정)
- **BE**: `./gradlew test` → **268 통과 / 0 실패 / 1 skip**(+운영시간·시급이력 2). 프로덕션 jar 빌드 OK.
- **FE**: `npx tsc --noEmit` → **0** · `npx jest` → **221 통과 / 0 실패** · `npx eslint` → **0 error**(경고 1242, 범위 외).

## 3. 대기 Worklist (구현 대상)

### 3-A. 죽은 버튼 8건 (버튼 인벤토리 에이전트 완료, 전부 기존 라우트로 배선 가능)
| 파일 | 컨트롤 | 조치 |
|---|---|---|
| settings/SettingsScreen.tsx:44 | "알림" | → navigate('NotificationSettings') |
| settings/SettingsScreen.tsx:45 | "화면 표시" | → AccountSettings의 테마토글로 이동 or 'AccountSettings' |
| settings/SettingsScreen.tsx:46 | "고객지원" | → 'QnA' 라우트 |
| home/components/LaborInfoBoard.tsx:50 | "더보기"(console.log) | → navigate('InfoList') |
| home/components/TaxInfoBoard.tsx:35 | "더보기"(핸들러X) | → navigate('InfoList') |
| home/components/TipsBoard.tsx:58 | "더보기"(핸들러X) | → navigate('InfoList') (TIPS 필터) |
| attendance/AttendanceScreen.tsx:569 | 상세 placeholder | → 'AttendanceCalendar'로 폴백 |
| attendance/MissingAttendanceCenterScreen.tsx:127 | "수동 기록" placeholder | → 'Attendance'로 이동 |
> 키-레디 OK(배선 불필요): SubscribeScreen:141·TossBillingAuthScreen:192(결제 SDK 대기).

### 3-B. 디자인/픽셀 QA (PM 에이전트 a19fc0fa…) — ✅ 완료
> 총평: 59화면 중 51개 ScreenContainer 채택(양호). 실기기 깨짐은 **2개 화면에 집중** + 반복 패턴(매장명/금액 numberOfLines 누락).
- **P0(실기기 깨짐)**:
  1. `myPage/PersonalUserScreen.tsx` — **전면 미마이그레이션**(하드코딩색 74곳·다크모드 전무·`Dimensions.get` 모듈레벨 dead·floatingTimer top:100 노치침범·매장명 numberOfLines 없음). → ScreenContainer+AppCard+토큰 재작성(큰 작업).
  2. `attendance/AttendanceScreen.tsx` — 레거시 COLORS·`#fff/#555` 하드코딩·**headerTitle 스타일 버그**(그라디언트 위 흰 박스, L900-905)·`workplaceSelector`(L682) row 오버플로(칩 잘림→horizontal ScrollView). 
  3. `welcome/OnboardingCarouselScreen.tsx:166` — illustrationBox 220×220 고정 → useResponsive 축소.
- **P1(일관성)**: MasterMyPage 레거시 COLORS(SODAM_BLUE/GREEN→토큰)+매장명/주소/금액 numberOfLines; OwnerDashboard Metric 금액 `numberOfLines+adjustsFontSizeToFit`; SalaryList 이름/금액 잘림; StoreDetail:67 storeName numberOfLines; 이모지→Ionicons(폴리시).
- **P2**: 모달 닫기 ✕ hitSlop(44pt), 하드코딩 #FF6B35→brandPrimary, AppText에 MoneyText/NameText 변종.
- **반복 패턴 일괄 적용**: 매장명=`numberOfLines={1} flexShrink:1`, 금액=`numberOfLines={1} adjustsFontSizeToFit`.

### 3-C. 기능 백로그 (PO ad3885d9 + 소상공인 a6bcd41a) — ✅ 완료 / 구현 진행 중
> 공통 결론: **화면은 다 있는데 사장이 닿는 진입버튼이 끊겨 있다 + 운영시간 쓰기 엔드포인트 부재**(C-5 푸시까지 연쇄 차단).
- **BE ✅ 완료(메인)**: 운영시간 `GET/PUT /api/stores/{id}/operating-hours`(끊긴 코어갭 복구→C-5 푸시·운영시간외 경고 살아남) + 시급이력 `GET /api/wages/store/{id}/history`(WageSettings 404 해소). 테스트 통과.
- **FE 진행 중(a6100261…)**: ①MasterMyPage 빠른메뉴 4버튼 죽음→배선(매출은 Non-Goal→재라벨/제거) ②StoreDetail 진입버튼(시급정책·운영시간·직원상세·초대코드) ③운영시간 편집 UI(신규) ④EmployeeDetail 개별시급(토스트만→`POST /api/wages/employee`) ⑤StoreEdit 주소변경(`PUT /stores/{id}/location`) ⑥PayrollRun storeId 자동전달 ⑦죽은버튼8 ⑧numberOfLines 일괄.
- **P1**: FCM FE SDK(실키), 시급 effectiveFrom 선택, 직원정보 수정 400리스크, 출퇴근정정 BE(직원측 mock), PAID처리/송금.
- **제외(Non-Goal/법)**: POS·매출분석·재고·채용·다국어·주민번호저장·세무/노무 대행·4대보험 EDI.

### 3-D. 매장 설정 페이지 (사용자 강조) — BE 존재 확인, FE 완성도 점검 필요
- **BE 있음(재사용)**: 매장 기본시급 `PUT /api/wages/store/{storeId}/standard`; 직원 개별시급 `POST /api/wages/employee`(미설정 시 매장 기본 폴백 `EmployeeStoreRelation.getAppliedHourlyWage`); 매장 정보(이름·주소·업종·반경·**storeStandardHourWage**) `PUT /api/stores/{storeId}` (`StoreUpdateDto`); 직원 활성토글 `PUT /api/stores/{storeId}/employees/{employeeId}/active`(이번 세션 신규).
- **FE 있음**: `StoreEditScreen`(매장 정보 편집, 라우트 등록), `WageSettingsScreen`, `EmployeeDetailScreen`(개별시급·활성토글).
- **점검 필요**: StoreEdit가 시급+주소+영업시간까지 커버·저장되는지, WageSettings 라우트 등록·진입 동선, StoreDetail→설정 진입 버튼. FE 구현 에이전트가 검증·보완.

## 4. 이번 세션 완료 (요약)
- BE 수익화: 4티어(FREE0/STARTER9900/PRO19900/PREMIUM39900)·BillingCycle·PAUSED·일시정지/재개·결제 멱등·PAST_DUE 재시도.
- BE 게이팅: `@RequirePlan`+`PlanGuardAspect`+`PlanAccessService`→402; 4대보험·집계뷰·명세서PDF(워터마크) 부착.
- BE 결제: 토스 라이브(정기) 검증 + **단건결제**(`TossPaymentGateway`·`TaxServiceOrder` 대리수취) key-ready + 웹훅→구독활성화.
- BE 노무/집계: 4대보험 신고서(합법·주민번호 미저장)·`LaborAggregationService`(인건비·연차·퇴직금, 가짜데이터 실집계 교체)·`SeveranceCalculator`·90일 슬립(`AccountSleepService`)·직원 활성토글.
- FE: 4티어/페이월/결제창 key-ready·미등록 라우트6 가드·salaryService(/salary/*) 데드 삭제→payroll 재배선·빈 버튼5·OwnerDashboard 에러UI·eslint 469→0.
- Flyway V10/V11/V12(작성만, 실행=인간승인). 권한: `.claude/settings.local.json` acceptEdits+광범위 allow(deny 안전망 유지).

## 5. 동기화할 문서 (변경 시 같이 갱신)
PRD.md·PRD_OWNER 등·FEATURES.md(출시차단)·수익화확정안 §9 상태표·출시-준비도-종합.md·결제-라이브-전환.md·엔드포인트-점검-매트릭스.md·**이 문서**.

## 6. 절대 라인 (위반 금지)
- ❌ 주민번호 저장·4대보험 EDI 대행·세무/노무 대행(법). ❌ POS/재고/채용/다국어(Non-Goal).
- ⚠️ 인간/외부 게이트(코드만, 실행 보류): 운영배포·토스 라이브전환·마이그레이션 실행·실키·외부계약·약관 법률문구·Play Store.

## 7. 활성 에이전트 (스냅샷)
| ID | 역할 | 상태 |
|---|---|---|
| a4f7ae40… | 버튼 인벤토리(읽기) | ✅ 완료 → §3-A |
| a19fc0fa… | 디자인/픽셀 QA(읽기) | ✅ 완료 → §3-B |
| ad3885d9… | PO 백로그(읽기) | ✅ 완료 → §3-C |
| a6bcd41a… | 소상공인 페르소나(읽기) | ✅ 완료 → §3-C |
| a6100261… | FE 구현(사장 플로우 배선+매장설정+죽은버튼+디자인P0) | ✅ 완료 → §8 |
| a95c68d2… | FE 디자인 픽셀수정(AttendanceScreen 헤더버그·칩오버플로·색토큰화 + 온보딩 반응형) | ✅ 완료 |
> **전 에이전트 트랙 완료.** 최종: BE 268·FE tsc0/jest221/eslint0. 사장 플로우·매장설정·운영시간·디자인 픽셀 P0 모두 닫음.
> **잔여 P2(미착수·문서화·출시 차단 아님):** PersonalUserScreen 전면 DS 마이그레이션(74색, 개인플로우·대형), MasterMyPage legacy 팔레트 전체 스왑(대형). 다크모드 폴리시 항목.

## 8. FE 사장 플로우 배선 결과 (a6100261… 완료)
**게이트**: tsc 0 · eslint 0 errors(1252 warnings, 기존) · jest 221 pass / 235(14 skip) — GREEN 유지.

**P0 (완료)**
- MasterMyPage 빠른메뉴 4버튼 배선(직원→StoreDetail·근태→Attendance·급여→PayrollRun(storeId)·매출분석→**대시보드 OwnerDashboard 로 리라벨**, POS Non-Goal 제거).
- StoreDetail: 매장관리 섹션 추가 — 시급정책(WageSettings)·운영시간(StoreOperatingHours)·직원초대(InviteShareSheet, storeCode 공유/복사)·편집(StoreEdit).
- StoreOperatingHours **신규 화면** — 7요일 오픈/마감(HH:mm)+휴무 토글, GET/PUT /api/stores/{id}/operating-hours. HomeNavigator+types.ts 등록.
- EmployeeDetail 시급변경: 토스트 stub → WageEditSheet + wageService.upsertEmployeeWage(customHourlyWage; 빈값=매장기본).
- StoreEdit: 매장주소 입력 + 주소검색(좌표 목, 카카오키 발급 전 폴백) → PUT /api/stores/{id}/location(fullAddress 포함). storeService.putLocation 시그니처 확장.
- PayrollRun: 손입력 매장ID → **매장 셀렉터**(useAuth+getMasterStores, param/첫매장 기본선택). 목록 미로딩 시 ID 입력 폴백. 테스트에 useAuth/storeService 목 추가.

**P1 (완료)**
- 죽은버튼: Settings(알림→NotificationSettings·화면표시→AccountSettings·고객지원→QnA), Labor/Tax/TipsBoard 더보기→InfoList, AttendanceScreen 상세→AttendanceCalendar, MissingAttendanceCenter 수동기록→Attendance.
- 디자인: 매장명 numberOfLines=1+flexShrink / 금액 numberOfLines=1+adjustsFontSizeToFit — MasterMyPage·StoreDetail·SalaryList·OwnerDashboard Metric.

**P2 (잔여 — 미착수, 문서화만)**
- AttendanceScreen P0 디자인: headerTitle 흰박스 버그(~L900)·workplaceSelector 가로 ScrollView(~L682)·하드코딩 #fff/#555→useThemeColors.
- OnboardingCarousel 220px 박스 useResponsive 축소.
- PersonalUserScreen 전체 DS 마이그레이션(74 하드코딩 색) — 대형.
- MasterMyPage 전체 legacy COLORS 팔레트→theme token 마이그레이션(빠른메뉴 SODAM_BLUE/GREEN 포함) — 화면 전체가 legacy 라 대형, P2.
