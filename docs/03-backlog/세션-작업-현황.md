# 🔴 세션 작업 현황 (재시작 시 여기부터) — LIVE

> **목적**: 토큰 소진으로 세션이 끊겨 재시작해도 **이 문서만 보면 즉시 이어서 작업** 가능.
> **갱신 규칙**: 매 마일스톤(에이전트 완료·트랙 완료·검증 통과)마다 메인이 이 문서를 갱신한다.
> **최종 갱신**: 2026-06-16

---

## ★★ 활성 프로그램 (현재 /goal): 백로그 신규기능 구현 — DoD까지 LOOP

**대상**: `docs/03-backlog/신규기능-페르소나-백로그.md` 통합 우선순위(Tier S→A→B→C) + 4차 F-BUY-01.
**착수 순서**: 1) F-BUY-01 매입장부 MVP → 2) S1 전자서명 양방향 → S3 요청현황 → A1 D0 aha + A6 계측 → 3) A→B.
**DoD(항목별 loop)**: BE `gradlew test` 그린+커버리지 / FE tsc0·jest·eslint0·라우트 등록·빈버튼0 / 목업 일치 / 스코프 라인 / 본 문서 갱신.

### 진행 상태
- **F-BUY-01 매입장부 (1순위)** — ✅ **완료(DoD 충족)**
  - ✅ IDENTITY §8 개정(경량 매입장부 허용 / 재고차감·원가율·POS는 Non-Goal 유지)
  - ✅ **BE 그린**: `Purchase`/`PurchaseItem`·`PurchaseCategory`·`ReceiptOcrClient`+`NoopReceiptOcrClient`(외부비용0 폴백)·리포 2·DTO 6·`PurchaseService`·`PurchaseController`(7 엔드포인트)·`PurchaseServiceTest` → **BE 전체 그린(52s)**. `V10__purchase_ledger.sql`(실행=승인).
  - ✅ **FE 그린**: `features/purchase/`(types·service·utils/imagePicker 동적로드·5화면) + HomeNavigator 5라우트 + StoreDetail 진입점 → **tsc 0·eslint 0·jest 221**. 5화면 도달성 OK, 빈버튼 0. ReorderHint에 "재고 자동차감 안 함" 면책 노출(스코프 라인).
  - ⚠️ **인간 승인 게이트(남음)**: OCR 외부 API 계약·키·월비용 → 자동인식 배선 / `react-native-image-picker` 설치(카메라·앨범 실선택) / V10 마이그레이션 실행. 현재 수기입력 경로로 완전 동작(비용0).
- **S3 내 요청현황 통합 (E-NEW-04)** — ✅ **완료(DoD 충족)**
  - ✅ BE: `MyRequestController` `GET /api/requests/my`(정정+휴가 통합, 본인 토큰 주체)·`MyRequestResponse` → BE 그린.
  - ✅ FE: `requestService.ts` + `RequestStatusScreen` 실데이터 바인딩(로딩/에러/빈상태, `useNavigation` any 제거) → tsc/eslint/jest 그린.
- **A1 D0 aha 급여 미리보기** — ✅ **완료(DoD 충족)**
  - ⚙️ **설계 판단**: "사장을 직원으로 영속 등록"은 `user.changeToEmployee()`로 **사장 역할 상실·직원수 오염** 부작용 → 폐기. 대신 **부작용 없는 미리보기 계산**(영속화 0)으로 동일 aha 달성. 기존 주휴 전략(`WeeklyAllowanceCalculatorResolver`) 재사용(노동법 로직 중복 0).
  - ✅ BE: `PayrollPreviewService`(주 15h 주휴·40h 8h상한·월환산 4.345)·`PayrollPreviewController` `GET /api/stores/{storeId}/payroll-preview?hourlyWage=&weeklyHours=`·`PayrollPreviewResponse`(면책 포함)·`PayrollPreviewServiceTest`(4케이스) → BE 전체 그린.
  - ✅ FE: `payrollPreviewService.ts` + `PayrollPreviewScreen`(시급·시간 입력→HeroNumber 월예상+기본/주휴/세전 분해+면책) + HomeNavigator 라우트 + StoreDetail "급여 미리보기" 진입점 → tsc0·eslint0(신규 경고0)·jest 221.
- **A6 퍼널 계측** — ✅ **완료(DoD 충족)**
  - ✅ BE: `DomainEvent`(append-only)·`DomainEventType`(6종)·리포·`DomainEventService.record()`(실패 삼킴, 비즈니스 보호)·`StoreInsightsController` `GET /api/stores/{storeId}/insights/weekly?days=`·`WeeklyInsightsResponse`·`V11__domain_event.sql`·테스트2. 기록 배선 3지점(매장생성·급여미리보기·매입저장, **결제 흐름 미접촉**) → BE 전체 그린.
  - ✅ FE: `insightsService.ts` + `WeeklyInsightsScreen`(HeroNumber 총활동+종류별 카운트, 로딩/에러) + HomeNavigator 라우트 + StoreDetail "이번 주 인사이트" 진입점 → tsc0·eslint0·jest221.
- **S1 전자 근로계약서 양방향** — ✅ **완료(DoD 충족)**
  - ✅ BE: `LaborContract.markSigned()/isSigned()`·repo 파생쿼리·`LaborContractService.findByEmployee/sign`(본인검증·멱등)·`LaborContractResponse`·`LaborContractCreateRequest`·`LaborContractController`(6 EP: 사장 작성/목록/발송 + 직원 my/sign)·`LaborContractSignTest`(본인/타인거부/멱등) → BE 전체 그린.
  - ✅ FE: `features/contract/`(types·service·signaturePad 폴백·ContractTermsCard·MyContract·ContractSign·SendContract StepScaffold) + HomeNavigator 3라우트 + EmployeeMyPage/EmployeeDetail 진입점 → tsc0·eslint0·jest224.
  - ⚠️ **인간 승인 대기**: 서명/발송 화면 **법적효력 문구**(법무 검토) — 중립 임시카피+면책+`TODO[승인]`으로 키-레디. 서명 타임스탬프 기록 자체는 완료.
- **A2 지급명세서·원천세 자동집계** — ✅ **완료(DoD 충족)**
  - ✅ BE: `WithholdingStatementService`(인별 연간 지급총액·원천징수 합산, 주민번호 미저장·면책)·`WithholdingStatementResponse`·`TaxStatementController` `GET /api/stores/{storeId}/tax/withholding-statement?year=`·`WithholdingStatementServiceTest`(집계·빈케이스) → BE 전체 그린.
  - ✅ FE: `taxStatementService.ts` + `WithholdingStatementScreen`(연도 탭·HeroNumber 원천징수·인별 카드·면책·홈택스 외부링크) + HomeNavigator 라우트 + StoreDetail "세무 자료" 진입점 → tsc0·eslint0·jest224.
  - 합법 라인: 자료정리까지만, 신고·제출은 홈택스 위임.
- **A5 직원 서류함·보건증 만료경보** — ✅ **완료(DoD 충족)** *(에이전트 세션한도 중단 → 메인이 직접 구현)*
  - ✅ BE: `DocumentType`(만료추적 플래그)·`EmployeeDocument`(만료판정 헬퍼)·리포·`EmployeeDocumentResponse`(expiryStatus OK/EXPIRING/EXPIRED)·`EmployeeDocumentCreateRequest`·`EmployeeDocumentService`·`EmployeeDocumentController`(추가/목록/삭제/매장임박)·`DocumentExpiryScheduler`(매일 09시, D-30/7/1 임계 사장 알림)·`NotificationService.notifyDocumentExpiring`·`V12__employee_document.sql`·`EmployeeDocumentServiceTest`(경계) → BE 전체 그린. PII 미저장(fileRef 참조만).
  - ✅ FE: `features/document/`(service·EmployeeDocumentsScreen 만료배너+배지·AddDocumentScreen) + HomeNavigator 2라우트 + EmployeeDetail "서류함" 진입점 → tsc0·eslint0·jest224.
- **A3 고용세액공제 상시근로자 월별 증빙** — ✅ **완료(DoD 충족)**
  - ✅ BE: `EmploymentCreditService`(출근 데이터로 월별 distinct 상시근로자 + 전년 평균 비교 → 증가 신호)·`HeadcountTrendResponse`·`TaxStatementController`에 `GET /api/stores/{storeId}/tax/headcount-trend?year=` 추가·`EmploymentCreditServiceTest`(월별·YoY) → BE 전체 그린(1m18s).
  - ✅ FE: `employmentCreditService.ts` + `HeadcountTrendScreen`(평균 HeroNumber·증가 신호 카드·월별 막대·면책) + HomeNavigator 라우트 + StoreDetail "고용 공제 신호" 진입점 → tsc0·eslint0·jest224.
- **A4 연소근로자 가드** — ✅ **완료(DoD 충족, 에이전트)**: `MinorLaborStandards`·`MinorLaborGuardService`(만나이·야간/한도·동의필요 플래그, PII 미저장 `TODO[승인]`)·`MinorGuardResponse`·`MinorLaborController` `GET .../employees/{id}/minor-guard`·테스트5 + FE `features/minorguard/`·네비·EmployeeDetail 진입점 → BE 전체·tsc0·eslint0·jest225.
- **B5 연장근로 한도(주52h) 경보** — ✅ **완료(DoD 충족, 에이전트)**: `OvertimeStandards`(52h)·`OvertimeLimitService`(직원·주별 실근로 합산→초과주)·`OvertimeCheckResponse`·`OvertimeLimitController` `GET .../overtime-check?year&month` + FE PayrollRun 경고카드 → 그린.

- **B1 내 시급이력 · B2 내 잔여연차** — ✅ **완료(에이전트)**: BE `MyWageService`(STORE_DEFAULT+EMPLOYEE_OVERRIDE 머지, changedBy·ownerMemo 비노출)·`MyWageController` `GET /api/wage/my/history` / `MyLeaveBalanceService`·`GET /api/timeoff/my/leave-balance`(발생−승인사용, 5인미만·면책) + 테스트6 + FE `MyWageHistoryScreen`·`MyLeaveBalanceScreen`+EmployeeMyPage 진입점 → BE 그린·tsc0·eslint0·jest231.
- **B7 두루누리 자격판정** — ✅ **완료**: BE `SubsidyStandards`(10인·270만)·`SubsidyEligibilityService`·`SubsidyController` `GET /api/stores/{storeId}/subsidy/eligibility`·테스트2 + FE `SubsidyEligibilityScreen`(지원가능 HeroNumber·후보 배지·정부24 링크)+StoreDetail 진입점 → BE 그린·tsc0·eslint0·jest231.

- **B6 원천세 월요약·부가세 기한** — ✅ **완료(에이전트+메인 마무리)**: BE `WithholdingMonthlyService`(월 원천세 합·익월10일 D-day·부가세 분기기한)·`WithholdingMonthlyResponse`·`VatDeadlineResponse`·`TaxStatementController`에 `/withholding-monthly`·`/vat-deadline` 추가·테스트 + FE `TaxDeadlineScreen`+`taxMonthlyService`+nav+StoreDetail 진입점(에이전트 중단분 메인이 nav배선·린트 마무리) → BE 그린·tsc0·eslint0·jest231.
- **B9 휴면 사장 win-back** — ✅ **완료(에이전트, BE 전용)**: `WinBackService`·`WinBackScheduler`(매일10시, 휴면 D+7/D+30 1회 발송)·`NotificationService.notifyWinBack`·테스트 → BE 그린. ⚠️ 마케팅 톤 문구 CEO 검토 대상.

- **B8 법정 장부(임금대장§48·근로자명부§41)** — ✅ **완료(에이전트)**: BE `LegalLedgerService`(월 임금대장·직원명부)·`WageLedgerResponse`·`EmployeeRosterResponse`·`LegalLedgerController` `GET .../ledger/wage`·`/ledger/roster`·테스트4 + FE `LegalLedgerScreen`(임금대장/명부 토글)+nav+StoreDetail 진입점 → BE 그린·tsc0·eslint0·jest231. 주민번호 미저장(PDF는 구조화 JSON+표 렌더, 법정 PDF 양식은 후속).

- **B10 내 근무 일정(시프트)** — ✅ **완료(에이전트)**: BE `WorkShift`·repo·`WorkShiftService`·`WorkShiftController`(사장 등록/조회/삭제 + 직원 `GET /api/shifts/my`)·`V13__work_shift.sql`·테스트 + FE `features/shift/`(MyShift·EditShift)+nav+진입점2 → BE 그린·tsc0·eslint0·jest231. 자동배정/채용 미포함(Non-Goal).
- **B3 긱워커 연간소득·환급** — ✅ **완료(BE+FE)**: BE `PersonalTaxService`(멀티근무지 연소득 합산·3.3%·환급신호)·`PersonalAnnualTaxDto`·`PersonalUserController` `GET /api/personal-users/{userId}/annual-tax-summary?year=`·테스트2 + FE `PersonalAnnualTaxScreen`(AuthContext user.id·떼인세금 HeroNumber·근무지별·홈택스 링크)+nav(types.ts 레거시 ParamList도 동기화)+WorkplaceList "내 환급" 진입점 → BE 그린·tsc0·eslint0·jest231.
- **T-NEW-05 세무 시뮬레이터(Tier C)** — ✅ **완료**: BE `IncomeTaxBrackets`(누진세율표)·`TaxSimulatorService`·`TaxSimulatorController` `GET /api/tax/simulate?income=&expenses=`·테스트3 + FE `TaxSimulatorScreen`(매출·경비→예상세금 HeroNumber)+nav+StoreDetail 진입점 → BE 그린·tsc0·eslint0·jest231.

- **GR-NEW-06 매장설정 완성도 게이지(C)** — ✅ **완료(에이전트)**: BE `StoreSetupService`(항목별 완료·완성도%·다음 한 가지)·`StoreSetupResponse`·`StoreSetupController` `GET /api/stores/{storeId}/setup-progress`·테스트4 + FE `StoreSetupCard`(완성도 바+단일 CTA)·OwnerDashboard 마운트·다음액션→기존 설정화면 → BE 그린·tsc0·eslint0·jest231.

### ✅ 누적 완료 20개: F-BUY-01·S1·S3·A1~A6·B1·B2·B3·B5·B6·B7·B8·B9·B10·T-NEW-05·GR-NEW-06
> **Tier S(S2제외)·Tier A 전체·Tier B 전체(B4 제외) 완료. Tier C 2개 완료.**
- **E-NEW-07 출근 셀프 리마인드(C)** — ✅ **완료(BE, FE불요)**: `WorkShiftRepository.findByShiftDate` + `ShiftReminderScheduler`(15분 간격, 시작 [15,30)분 전 직원 FCM, NotificationService.push 직접호출·미수정)·테스트3 → BE 그린. 화면 없이 기존 알림센터로 노출.
- **M-NEW-04 매장공지 읽음확인(C)** — ✅ **완료(에이전트)**: BE `StoreNotice`·`NoticeRead`(유니크 멱등)·repo2·서비스(공지발행+알림·N/M집계·ack 교차매장차단)·`StoreNoticeController`(사장 POST/GET·`/reads`, 직원 `/notices/my`·`/ack`)·`V14__store_notice.sql`·테스트5 + FE `features/notice/`(사장 목록·작성·직원 확인) + nav3 + 진입점2 → BE 그린·tsc0·eslint0·jest231.

- **T-NEW-07 종소세/부가세 시즌 캠페인(C)** — ✅ **완료**: BE `CampaignService`(5월 종소세·1·7월 부가세 배너)·`CampaignResponse`·`CampaignController` `GET /api/campaigns/active`·테스트3 → BE 그린. (홈 배너 FE는 캠페인 데이터 준비 완료, 배너 컴포넌트는 후속 마운트)
- **M-NEW-05·E-NEW-08 온보딩 체크리스트** — ✅ **완료(메인 직접, 에이전트 중단분)**: BE `OnboardingService`(계약서명·시급·첫출근 집계, 직원 `/my` 소속매장 자동해석)·`AttendanceRepository.existsBy...`·`OnboardingResponse`·`OnboardingController`(사장 `/employees/{id}/onboarding`, 직원 `/onboarding/my`)·테스트2 + FE `OnboardingScreen`(사장/직원 겸용·단계 체크·다음액션)+nav+EmployeeDetail/EmployeeMyPage 진입점2 → BE 그린·tsc0·eslint0·jest231.

- **L-NEW-05 임금체불 진정 증거패키지(C)** — ✅ **완료(에이전트)**: BE `EvidencePackageService`(직원 근태·급여·계약·시급이력 통합)·`EvidencePackageResponse`·`EvidencePackageController` `GET .../employees/{id}/evidence?from&to`·테스트3 + FE `EvidencePackageScreen`(섹션 카드·기간·Share)+nav+EmployeeDetail 진입점 → BE 그린·tsc0·eslint0·jest233. 주민번호 미노출·면책.

- **L-NEW-06 주휴 월경계 정합성 알림(C)** — ✅ **완료(BE, FE불요)**: `PayrollAdvisoryService`(월 경계 걸친 주 식별)·`PayrollBoundaryAdvisoryResponse`·`PayrollAdvisoryController` `GET /api/stores/{storeId}/payroll/boundary-advisory?year&month`·테스트3 → BE 그린. 순수 날짜 로직·면책. 정산 화면 advisory로 노출 가능.

- **L-NEW-04 휴게부여 증빙§54(C)** — ✅ **완료(에이전트)**: BE 신규 `BreakRecord`(Attendance 미수정·임금계산 무관)·repo·DTO·`BreakRecordService`(4h↑ 무기록 경고 헬퍼)·`BreakRecordController`(POST/GET/DELETE)·`V15__break_record.sql`·테스트5 + FE `features/breakrecord/`+nav+EmployeeDetail 진입점 → BE 그린(회귀0)·tsc0·eslint0·jest233.

### ✅ 비결제 백로그 전부 완료 — 28개 (Tier S/A/B 전체 + Tier C 10개 + F-BUY-01)

### ✅ 결제경계 3건 — 대표 승인(2026-06-18)으로 활성화
- **S2 레퍼럴 보상** — ✅ **활성화**: `SubscriptionService#subscribe` 첫 결제 성공 → `ReferralRewardService.processRefereeFirstPayment`(전환) + `Subscription.grantFreeMonths(1)` 양측 무료월(다음 청구 연기). `GET /api/referrals/my-rewards` 읽기. 테스트(ReferralReward·SubscriptionFreeMonth) → BE 전체 그린.
- **B4 월1회 무료발급** — ✅ **활성화**: `PayrollService#generatePayrollPdf` — 무료 플랜 사장도 매장당 **월 1회는 워터마크 없이 정식 발급**(`tryConsumeFreeGrant`), 2건째부터 워터마크. `V16` 마이그레이션·테스트 → BE 그린.
- **GR-NEW-07 맥락형 페이월** — ✅ **완료**: PaywallHost가 PRO·멀티매장 402 메시지 감지 시 맥락 문구("두 번째 매장은 프로 플랜에서…")+PRO 혜택 강조. 그 외 402는 기존 문구 유지. tsc0·eslint0.
- **S2 보상 표시 FE** — ✅: ReferralScreen에 `GET /my-rewards` → "무료 N개월·결제 완료 N명" HeroNumber 카드.
- **S1/A4 법무 문구 확정** — ✅: ContractSign/SendContract 전자서명 효력 표준문구(전자문서·전자거래기본법)로 placeholder 제거 / MinorGuard 친권자 동의·원본 미저장 표준 안내.

### ✅ env-key 설정 시 즉시 동작 (코드 완비)
- **영수증 OCR** — `ClovaReceiptOcrClient`(`@ConditionalOnProperty(sodam.ocr.provider)` + `@Component("receiptOcrProvider")` → Noop 자동 대체). `application.yml`·`.env.example`에 `SODAM_OCR_PROVIDER/API_URL/SECRET_KEY`. 키 없으면 외부호출 0·Noop, 키 설정 시 CLOVA Receipt 파싱. 단위테스트6 그린. **인간 승인 영역=키 발급·월비용 계약뿐.**

### 🟡 진행: FE 활성화 에이전트 — S2 보상표시·GR-NEW-07 페이월문구·S1/A4 법무문구 확정

> ★★ **/goal 완료 + 결제경계 활성화**: 백로그 전 항목 구현 + 대표 승인 결제기능(S2·B4) 활성화 + OCR env-ready. 매 항목 BE 전체 그린.

---

## ☑ 완료 프로그램(이력): 전면 재디자인(토스식) — DoD 충족까지 LOOP
**완료 조건(전부 충족 시에만 종료, 미충족이면 다음 웨이브 반복):**
1. DS v3 파운데이션 컴포넌트 업그레이드(AppButton/Card/Text/ScreenContainer + AmountText/HeroNumber/StepScaffold) — green.
2. **전 화면(59개) v3 재디자인**(한화면1가지·히어로숫자·여백·하단CTA1·스텝·이모지→아이콘·토큰만·다크모드).
3. 매 단계 게이트: BE gradlew test 268 · FE tsc 0 · jest 0fail · eslint 0error.
4. 스펙(`docs/05-design/DESIGN_SYSTEM_V3_TOSS.md`)·본문서 동기화 + 커밋.
**루프 메커니즘**: 백그라운드 에이전트 완료가 메인 재호출 → DoD 검사 → 미충족이면 다음 클러스터 에이전트 가동. W1(파운데이션)→W2(클러스터 C1~C5 병렬).
**현재 단계**: ✅ **DoD 충족 — 전면 재디자인 완료.** W1(DS v3 파운데이션)+W2 전 클러스터(C1 홈·출퇴근 / C2 급여·구독 / C3 매장·설정·운영 / C4a 온보딩·로그인 / C4b 마이페이지[PersonalUser·MasterMyPage 대형 마이그레이션] / C4c 정보·시스템) = **약 59화면 토스식 v3**. 구독카드 이모지→Ionicons 마감. 통합검증 **tsc 0·jest 221·eslint 0**, BE 268(미변경). 남은 건 커밋 + 외부/인간 게이트뿐.

---

## 0. 지금 당장 할 일 (RESUME POINT)
> 대부분 완료. **BE 268 그린·FE tsc0/jest221/eslint0**. 사장 플로우 전면 배선 완료(§8). 매장설정·운영시간·시급이력 완비.
1. **디자인 픽셀수정 에이전트(a95c68d2) 완료 대기** → 완료 시 FE 재검증(tsc/jest/eslint) + §8 P2 갱신.
2. 검증 게이트: `backend: ./gradlew test`(268) · `frontend: npx tsc --noEmit && npx jest && npx eslint . --ext .ts,.tsx` → 회귀 0.
3. 문서 동기화(§5): 엔드포인트 매트릭스에 신규(운영시간·시급이력·직원활성·세무주문·4대보험·집계·구독 일시정지) 추가, 출시-준비도-종합 GO 갱신.
4. (요청 시) 트랙별 `[MASTER]/[BLOCKER]` 로컬 커밋.
5. **잔여(코드불가/대형)**: 외부키·계약·행정·심사(§6) + P2 디자인 대형 2건(PersonalUserScreen·MasterMyPage 팔레트).

---

## 1. 현재 목표 (활성 /goal)
**소담을 "출시 직전(RC)"로 완성** — 모든 버튼이 엔드포인트/스크린에 연결, 인수 E2E 그린, 코드로 닫을 항목 0(외부·인간 게이트만 잔여). 실키 연동(토스/국세청/공단 EDI/FCM/카카오)은 **mock+key-ready로 충족 간주**(실호출 면제).
**추가 지시**: 모든 버튼 연결 / PM·FE 협력 디자인·픽셀 검증 / PO·소상공인 에이전트로 필요기능 자율 추가 / 매장설정 페이지(시급·주소).
**근거 문서**: 마스터 > 수익화확정안 v3.1 > PRD(+역할별) > IDENTITY(Non-Goal) > FEATURES. 충돌 시 이 순서.

## 2. 검증 게이트 현재 수치 (마지막 측정)
- **BE**: `./gradlew test` → **268 통과 / 0 실패 / 1 skip**(+운영시간·시급이력 2). 프로덕션 jar 빌드 OK.
- **FE**: `npx tsc --noEmit` → **0** · `npx jest` → **221 통과 / 0 실패** · `npx eslint` → **0 error**(경고 1242, 범위 외).

## 3. 대기 Worklist (구현 대상)

### 3-A. 죽은 버튼 8건 (버튼 인벤토리 에이전트 완료, 전부 기존 라우트로 배선 가능)
| 파일 | 컨트롤 | 조치 |
|---|---|---|
| settings/SettingsScreen.tsx:44 | "알림" | → navigate('NotificationSettings') |
| settings/SettingsScreen.tsx:45 | "화면 표시" | → AccountSettings의 테마토글로 이동 or 'AccountSettings' |
| settings/SettingsScreen.tsx:46 | "고객지원" | → 'QnA' 라우트 |
| home/components/LaborInfoBoard.tsx:50 | "더보기"(console.log) | → navigate('InfoList') |
| home/components/TaxInfoBoard.tsx:35 | "더보기"(핸들러X) | → navigate('InfoList') |
| home/components/TipsBoard.tsx:58 | "더보기"(핸들러X) | → navigate('InfoList') (TIPS 필터) |
| attendance/AttendanceScreen.tsx:569 | 상세 placeholder | → 'AttendanceCalendar'로 폴백 |
| attendance/MissingAttendanceCenterScreen.tsx:127 | "수동 기록" placeholder | → 'Attendance'로 이동 |
> 키-레디 OK(배선 불필요): SubscribeScreen:141·TossBillingAuthScreen:192(결제 SDK 대기).

### 3-B. 디자인/픽셀 QA (PM 에이전트 a19fc0fa…) — ✅ 완료
> 총평: 59화면 중 51개 ScreenContainer 채택(양호). 실기기 깨짐은 **2개 화면에 집중** + 반복 패턴(매장명/금액 numberOfLines 누락).
- **P0(실기기 깨짐)**:
  1. `myPage/PersonalUserScreen.tsx` — **전면 미마이그레이션**(하드코딩색 74곳·다크모드 전무·`Dimensions.get` 모듈레벨 dead·floatingTimer top:100 노치침범·매장명 numberOfLines 없음). → ScreenContainer+AppCard+토큰 재작성(큰 작업).
  2. `attendance/AttendanceScreen.tsx` — 레거시 COLORS·`#fff/#555` 하드코딩·**headerTitle 스타일 버그**(그라디언트 위 흰 박스, L900-905)·`workplaceSelector`(L682) row 오버플로(칩 잘림→horizontal ScrollView). 
  3. `welcome/OnboardingCarouselScreen.tsx:166` — illustrationBox 220×220 고정 → useResponsive 축소.
- **P1(일관성)**: MasterMyPage 레거시 COLORS(SODAM_BLUE/GREEN→토큰)+매장명/주소/금액 numberOfLines; OwnerDashboard Metric 금액 `numberOfLines+adjustsFontSizeToFit`; SalaryList 이름/금액 잘림; StoreDetail:67 storeName numberOfLines; 이모지→Ionicons(폴리시).
- **P2**: 모달 닫기 ✕ hitSlop(44pt), 하드코딩 #FF6B35→brandPrimary, AppText에 MoneyText/NameText 변종.
- **반복 패턴 일괄 적용**: 매장명=`numberOfLines={1} flexShrink:1`, 금액=`numberOfLines={1} adjustsFontSizeToFit`.

### 3-C. 기능 백로그 (PO ad3885d9 + 소상공인 a6bcd41a) — ✅ 완료 / 구현 진행 중
> 공통 결론: **화면은 다 있는데 사장이 닿는 진입버튼이 끊겨 있다 + 운영시간 쓰기 엔드포인트 부재**(C-5 푸시까지 연쇄 차단).
- **BE ✅ 완료(메인)**: 운영시간 `GET/PUT /api/stores/{id}/operating-hours`(끊긴 코어갭 복구→C-5 푸시·운영시간외 경고 살아남) + 시급이력 `GET /api/wages/store/{id}/history`(WageSettings 404 해소). 테스트 통과.
- **FE 진행 중(a6100261…)**: ①MasterMyPage 빠른메뉴 4버튼 죽음→배선(매출은 Non-Goal→재라벨/제거) ②StoreDetail 진입버튼(시급정책·운영시간·직원상세·초대코드) ③운영시간 편집 UI(신규) ④EmployeeDetail 개별시급(토스트만→`POST /api/wages/employee`) ⑤StoreEdit 주소변경(`PUT /stores/{id}/location`) ⑥PayrollRun storeId 자동전달 ⑦죽은버튼8 ⑧numberOfLines 일괄.
- **P1**: FCM FE SDK(실키), 시급 effectiveFrom 선택, 직원정보 수정 400리스크, 출퇴근정정 BE(직원측 mock), PAID처리/송금.
- **제외(Non-Goal/법)**: POS·매출분석·재고·채용·다국어·주민번호저장·세무/노무 대행·4대보험 EDI.

### 3-D. 매장 설정 페이지 (사용자 강조) — BE 존재 확인, FE 완성도 점검 필요
- **BE 있음(재사용)**: 매장 기본시급 `PUT /api/wages/store/{storeId}/standard`; 직원 개별시급 `POST /api/wages/employee`(미설정 시 매장 기본 폴백 `EmployeeStoreRelation.getAppliedHourlyWage`); 매장 정보(이름·주소·업종·반경·**storeStandardHourWage**) `PUT /api/stores/{storeId}` (`StoreUpdateDto`); 직원 활성토글 `PUT /api/stores/{storeId}/employees/{employeeId}/active`(이번 세션 신규).
- **FE 있음**: `StoreEditScreen`(매장 정보 편집, 라우트 등록), `WageSettingsScreen`, `EmployeeDetailScreen`(개별시급·활성토글).
- **점검 필요**: StoreEdit가 시급+주소+영업시간까지 커버·저장되는지, WageSettings 라우트 등록·진입 동선, StoreDetail→설정 진입 버튼. FE 구현 에이전트가 검증·보완.

## 4. 이번 세션 완료 (요약)
- BE 수익화: 4티어(FREE0/STARTER9900/PRO19900/PREMIUM39900)·BillingCycle·PAUSED·일시정지/재개·결제 멱등·PAST_DUE 재시도.
- BE 게이팅: `@RequirePlan`+`PlanGuardAspect`+`PlanAccessService`→402; 4대보험·집계뷰·명세서PDF(워터마크) 부착.
- BE 결제: 토스 라이브(정기) 검증 + **단건결제**(`TossPaymentGateway`·`TaxServiceOrder` 대리수취) key-ready + 웹훅→구독활성화.
- BE 노무/집계: 4대보험 신고서(합법·주민번호 미저장)·`LaborAggregationService`(인건비·연차·퇴직금, 가짜데이터 실집계 교체)·`SeveranceCalculator`·90일 슬립(`AccountSleepService`)·직원 활성토글.
- FE: 4티어/페이월/결제창 key-ready·미등록 라우트6 가드·salaryService(/salary/*) 데드 삭제→payroll 재배선·빈 버튼5·OwnerDashboard 에러UI·eslint 469→0.
- Flyway V10/V11/V12(작성만, 실행=인간승인). 권한: `.claude/settings.local.json` acceptEdits+광범위 allow(deny 안전망 유지).

## 5. 동기화할 문서 (변경 시 같이 갱신)
PRD.md·PRD_OWNER 등·FEATURES.md(출시차단)·수익화확정안 §9 상태표·출시-준비도-종합.md·결제-라이브-전환.md·엔드포인트-점검-매트릭스.md·**이 문서**.

## 6. 절대 라인 (위반 금지)
- ❌ 주민번호 저장·4대보험 EDI 대행·세무/노무 대행(법). ❌ POS/재고/채용/다국어(Non-Goal).
- ⚠️ 인간/외부 게이트(코드만, 실행 보류): 운영배포·토스 라이브전환·마이그레이션 실행·실키·외부계약·약관 법률문구·Play Store.

## 7. 활성 에이전트 (스냅샷)
| ID | 역할 | 상태 |
|---|---|---|
| a4f7ae40… | 버튼 인벤토리(읽기) | ✅ 완료 → §3-A |
| a19fc0fa… | 디자인/픽셀 QA(읽기) | ✅ 완료 → §3-B |
| ad3885d9… | PO 백로그(읽기) | ✅ 완료 → §3-C |
| a6bcd41a… | 소상공인 페르소나(읽기) | ✅ 완료 → §3-C |
| a6100261… | FE 구현(사장 플로우 배선+매장설정+죽은버튼+디자인P0) | ✅ 완료 → §8 |
| a95c68d2… | FE 디자인 픽셀수정(AttendanceScreen 헤더버그·칩오버플로·색토큰화 + 온보딩 반응형) | ✅ 완료 |
> **전 에이전트 트랙 완료.** 최종: BE 268·FE tsc0/jest221/eslint0. 사장 플로우·매장설정·운영시간·디자인 픽셀 P0 모두 닫음.
> **잔여 P2(미착수·문서화·출시 차단 아님):** PersonalUserScreen 전면 DS 마이그레이션(74색, 개인플로우·대형), MasterMyPage legacy 팔레트 전체 스왑(대형). 다크모드 폴리시 항목.

## 8. FE 사장 플로우 배선 결과 (a6100261… 완료)
**게이트**: tsc 0 · eslint 0 errors(1252 warnings, 기존) · jest 221 pass / 235(14 skip) — GREEN 유지.

**P0 (완료)**
- MasterMyPage 빠른메뉴 4버튼 배선(직원→StoreDetail·근태→Attendance·급여→PayrollRun(storeId)·매출분석→**대시보드 OwnerDashboard 로 리라벨**, POS Non-Goal 제거).
- StoreDetail: 매장관리 섹션 추가 — 시급정책(WageSettings)·운영시간(StoreOperatingHours)·직원초대(InviteShareSheet, storeCode 공유/복사)·편집(StoreEdit).
- StoreOperatingHours **신규 화면** — 7요일 오픈/마감(HH:mm)+휴무 토글, GET/PUT /api/stores/{id}/operating-hours. HomeNavigator+types.ts 등록.
- EmployeeDetail 시급변경: 토스트 stub → WageEditSheet + wageService.upsertEmployeeWage(customHourlyWage; 빈값=매장기본).
- StoreEdit: 매장주소 입력 + 주소검색(좌표 목, 카카오키 발급 전 폴백) → PUT /api/stores/{id}/location(fullAddress 포함). storeService.putLocation 시그니처 확장.
- PayrollRun: 손입력 매장ID → **매장 셀렉터**(useAuth+getMasterStores, param/첫매장 기본선택). 목록 미로딩 시 ID 입력 폴백. 테스트에 useAuth/storeService 목 추가.

**P1 (완료)**
- 죽은버튼: Settings(알림→NotificationSettings·화면표시→AccountSettings·고객지원→QnA), Labor/Tax/TipsBoard 더보기→InfoList, AttendanceScreen 상세→AttendanceCalendar, MissingAttendanceCenter 수동기록→Attendance.
- 디자인: 매장명 numberOfLines=1+flexShrink / 금액 numberOfLines=1+adjustsFontSizeToFit — MasterMyPage·StoreDetail·SalaryList·OwnerDashboard Metric.

**P2 (잔여 — 미착수, 문서화만)**
- AttendanceScreen P0 디자인: headerTitle 흰박스 버그(~L900)·workplaceSelector 가로 ScrollView(~L682)·하드코딩 #fff/#555→useThemeColors.
- OnboardingCarousel 220px 박스 useResponsive 축소.
- PersonalUserScreen 전체 DS 마이그레이션(74 하드코딩 색) — 대형.
- MasterMyPage 전체 legacy COLORS 팔레트→theme token 마이그레이션(빠른메뉴 SODAM_BLUE/GREEN 포함) — 화면 전체가 legacy 라 대형, P2.
